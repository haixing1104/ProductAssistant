# --- 环境与密钥（绝不入库）---
infra/.env

# --- 数据库备份（不入库）---
infra/backups/

# --- 演示素材母带（原始录屏 / 导出 mp4，几百 MB 级且可再生，不入库）---
# 作品集页面用的是压缩后的产物：portfolio/public/demos/<slug>/<platform>/<key>.<ext>
# 母带留在本机（或对象存储）即可；压缩命令见 portfolio/public/demos/pa/README.md
demo-index/
demo-index-mp4/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
htmlcov/
.coverage

# --- Node / 前端 ---
node_modules/
dist/
coverage/

# --- 编辑器 / 系统 ---
.idea/
.vscode/
.DS_Store

# --- 生产部署（infra/nginx）：证书与 ACME webroot 绝不入库 ---
# 证书由 certbot 在服务器上签发（或阿里云免费证书），私钥一旦入库等于公开；
# www/ 是 certbot --webroot 的挑战文件目录，属运行期产物。
infra/nginx/certs/
infra/nginx/www/

# --- 证书下载包与散落的私钥文件（**兜底**规则）---
# 2026-09-20 实际踩过：阿里云控制台下载的 <订单号>_<域名>_nginx.zip 直接落在**仓库根**，
# 包里就是私钥；而上面只忽略了 infra/nginx/certs/ 目录，一次 `git add .` 就会入库。
# 这里再按「仓库根」兜一道：`/<模式>` 只匹配根目录，不会误伤子目录里的合法素材。
/*.zip
/*.pem
/*.key
/*.crt
/*.pfx
/*.p12
