# --- 环境与密钥（绝不入库）---
infra/.env

# --- 数据库备份（不入库）---
infra/backups/

# --- 演示素材母带（原始录屏 / 导出 mp4，几百 MB 级且可再生，不入库）---
# 作品集页面用的是压缩后的产物：portfolio/public/demos/<slug>/<platform>/<key>.<ext>
# 母带留在本机（或对象存储）即可；压缩命令见 portfolio/public/demos/pa/README.md
demo-index/
demo-index-mp4/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
htmlcov/
.coverage

# --- Node / 前端 ---
node_modules/
dist/
coverage/

# --- 编辑器 / 系统 ---
.idea/
.vscode/
.DS_Store

# --- 生产部署（infra/nginx）：证书与 ACME webroot 绝不入库 ---
# 证书由 certbot 在服务器上签发（或阿里云免费证书），私钥一旦入库等于公开；
# www/ 是 certbot --webroot 的挑战文件目录，属运行期产物。
infra/nginx/certs/
infra/nginx/www/

# --- 内部文档（本地保留、**不发布**）---------------------------------------------
# 对外只发布根 README.md（功能与核心架构设计）；下面这些是"对内"的详细文档：
#   · 内部详细文档 README_INTERNAL.md —— 与 README.md 逐章对应，但含完整步骤与实测细节；
#   · 各模块的原始内部文档（契约/运行/测试/部署手册）—— 已合并进 README_INTERNAL.md，
#     原地保留一份便于随时回看。
# 2026-09-21 起从版本库移除，并已用 git-filter-repo 重写历史（旧提交里也不再有它们）。
# ⚠️ 新增文档前先想清楚：只要它出现在这里，就只活在开发机上。
/README_INTERNAL.md
/INTERVIEW_QUESTION_BANK.md
/backend-api/README.md
/frontend/README.md
/mobile-h5/README.md
/mobile-rn/README.md
/mobile-rn/docs/
/portfolio/README.md
/portfolio/public/demos/pa/README.md
/infra/docs/

# --- 证书下载包与散落的私钥文件（**兜底**规则）---
# 2026-09-20 实际踩过：阿里云控制台下载的 <订单号>_<域名>_nginx.zip 直接落在**仓库根**，
# 包里就是私钥；而上面只忽略了 infra/nginx/certs/ 目录，一次 `git add .` 就会入库。
# 这里再按「仓库根」兜一道：`/<模式>` 只匹配根目录，不会误伤子目录里的合法素材。
/*.zip
/*.pem
/*.key
/*.crt
/*.pfx
/*.p12
