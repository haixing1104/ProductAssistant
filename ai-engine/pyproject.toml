# 仅工具配置（不承载依赖；依赖以 requirements*.txt 为准）
# 包根是 src（PEP 420 命名空间包）：内部相对导入形如 src.workflowcore.graph / src.service，
# 因此 pythonpath 指向本目录，运行入口为 `python -m src.service`。
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
