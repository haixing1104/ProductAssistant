# ai-engine 运行依赖
# 规则：仅 stable、禁 pre-release；精确补丁在安装后冻结锁文件
#
# 已启用：langgraph / langgraph-checkpoint-postgres / redis / pydantic-settings / pydantic /
#         pymilvus（RAG/Milvus adapter 已落地，见 adapters/milvus_rag_store.py）/
#         pillow（图片规格化已落地，见 adapters/image_pillow.py）；
#
# 预留：langchain-openai（真实 LLM 走智谱标准库 adapter；embedding 亦走智谱 /embeddings 纯 stdlib，无需 OpenAI SDK）
langgraph>=1.2,<2.0
langgraph-checkpoint-postgres>=3.1,<4.0   # PostgresSaver（P4 启用，建表走 role_pa_ai_setup）
psycopg[binary]>=3.3,<3.4                  # 运行期 PQ 驱动（adapters/pg_store.py）；slim 镜像无 libpq，须装 binary
psycopg-pool>=3.2,<4.0                     # PostgresSaver 连接池（langgraph-checkpoint-postgres 的传递依赖，此处显式锁定）
langchain-openai>=1.6,<2.0                # 或 openai>=3.8,<4.0（二选一）
pymilvus>=2.6,<2.7                        # RAG 向量库（Milvus 2.6.x）
pillow>=11,<12                            # 图片规格化：统一尺寸/格式/背景 + AI 标识元数据（adapters/image_pillow.py）
redis>=8.1,<9.0                           # EventBus（Redis Streams，P4 启用）
pydantic>=2.13,<3.0
pydantic-settings>=2.6,<3.0
