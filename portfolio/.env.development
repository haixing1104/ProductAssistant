# ── 作品集宣传页 | dev 环境变量（Vite 只在 `vite dev` 时读本文件；`vite build` 读 .env / .env.production）──
#
# 作品卡标题行右侧的「开始使用」入口，在 dev 下直接指向本机跑着的业务前端，点了就到登录页：
#   · PC Web    → frontend/  :5173（strictPort，见 frontend/vite.config.ts）
#   · Mobile H5 → mobile-h5/ :5174（strictPort + host:true，见 mobile-h5/vite.config.ts）
#
# 想真的登进系统：先 `./scripts/dev-up.sh`（backend :8000 + 两个前端），再 `./scripts/dev-landing.sh`（本页 :5175）。
#
# 这两行**只属于 dev**：生产构建不读本文件，入口地址改由 `src/data/projects.ts` 的
# `links.live` / `links.liveH5` 提供（那条门禁要求 https，所以 http://localhost 进不了数据 ——
# 两个环境天然分层）。两处都为空时入口不渲染：页面绝不放一个点了 404 的按钮。
VITE_ENTRY_BASE_URL=http://localhost:5173
VITE_ENTRY_H5_BASE_URL=http://localhost:5174
